<%- column_div :type => :primary do -%>
  <h2><%=t ('group.group_person', :person => @person.name) %></h2>

  <% if current_person?(@person) %>
    <%= link_to t('group.add_group'), new_group_path() %>
  <% end %>
  <%= render :partial => 'groups', 
             :locals => {:groups => @groups, :some_groups => @some_groups,
                :title => t('group.joined_groups'), :link => "groups"} %>
  <%= render :partial => 'groups', 
             :locals => {:groups => @own_groups, :some_groups => @some_own_groups,
                :title => t('group.admin_groups'), :link => "admin"} %>
  
<%- end -%>

<%- column_div :type => :secondary do -%>
  <% params[:model] = "Group" %>
  <%= render :partial => 'searches/box' %>
<%- end -%>

<div class="clear"></div>